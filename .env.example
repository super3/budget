# Optional: overrides the default Clerk publishable key hardcoded in
# src/clerk.ts (e.g. to use a pk_live production instance). Publishable
# keys are safe to expose client-side. Copy to .env.local for local dev.
VITE_CLERK_PUBLISHABLE_KEY=
VITE_API_URL=

# Railway service config, used only if the service's Root Directory is
# set to `server`. The default setup needs no Root Directory at all —
# the repo-root railway.toml points build/start here instead.
[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
